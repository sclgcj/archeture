#ifndef HASH_HUB_H
#define HASH_HUB_H

/*
 * hub_add() - add a harbor to a link
 * @extra_data: user_data	
 *
 * Return: 0 if successful, -1 if not
 */
int
hash_hub_add(
	unsigned long user_data
);

#endif
